# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
.venv/
venv/
env/

# Build / dist
build/
dist/
*.egg

# IDEs / editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Tooling artifacts
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/hooks/*
!.claude/hooks/auto_claude_session_brief.sh
.superpowers/

# Runtime annotation sessions (these belong to the user, not the repo)
.spatial-annotations/

# uv
uv.lock

# Handoff dir: most contents are runtime data, but we track:
#   .gitkeep, .gitignore, README.md, state.example.json
# (the inner .handoff/.gitignore selects what to ignore per-machine)
